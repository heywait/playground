import React from 'react';
import '../../styles/reset.css';
import styles from './styles.css';

const Application = () => {
    return <div className={`${styles.root} ${styles.root_test}`}>test page</div>;
};

export default Application;
